// Input configuration:

[
  {
    "name": "sample_files",
    "label": "Sample CSV Files",
    "order": 1,
    "multiple": true,
    "required": true,
    "help_text": "Upload one or more Abbot Alinity/Architect Sample CSVs (hold Ctrl/Cmd to select several).",
    "field_type": "file",
    "max_size_mb": 50,
    "allowed_extensions": ["csv"]
  },
  {
    "name": "qc_files",
    "label": "QC (Control) CSV Files",
    "order": 2,
    "multiple": true,
    "required": true,
    "help_text": "Upload one or more Abbot Alinity/Architect QC CSVs (hold Ctrl/Cmd to select several).",
    "field_type": "file",
    "max_size_mb": 50,
    "allowed_extensions": ["csv"]
  },
  {
    "name": "batch_label",
    "order": 3,
    "label": "Batch/Run Label (optional)",
    "field_type": "text",
    "required": false,
    "help_text": "Optional short label (e.g. site name or batch number) to include in the output filename."
  }
]

  // Output configuration:
  [
  {
    "name": "alinity_report",
    "label": "Alinity Assay Summary Report",
    "format": "csv"
  }
]
