"""
Views for the Settings app.
Currently, all settings are managed via Django admin, so no public views are required.
This file is kept for potential future use (e.g., a custom settings management UI).
"""
